/**
 * Smoke test to verify the test runner is configured correctly.
 * Replace with real engine tests as they are implemented.
 */
describe('test runner', () => {
  it('should run a basic assertion', () => {
    expect(1 + 1).toBe(2)
  })
})
